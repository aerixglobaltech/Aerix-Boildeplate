// =============================================================================
// Configuration Barrel Export
// =============================================================================

export { clientEnv, serverEnv } from './env';
export { ROUTES, PROTECTED_ROUTES, AUTH_ROUTES, PUBLIC_ROUTES } from './routes';
